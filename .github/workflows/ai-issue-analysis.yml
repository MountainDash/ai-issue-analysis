name: AI Issue Analysis

on:
  issues:
    types: [opened]
  issue_comment:
    types: [created]
  workflow_dispatch:
    inputs:
      issue_number:
        description: "Issue number to analyze"
        required: true
        type: number

jobs:
  mimo-analysis:
    if: |
      (github.event_name == 'issues' && github.event.action == 'opened') ||
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'issue_comment' &&
       github.event.action == 'created' &&
       contains(github.event.comment.body, '@github-actions') &&
       github.event.comment.user.type != 'Bot')
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
      issues: write
    steps:
      # 推荐配套：
      # - .claude/skills/generic-issue-log-analysis/SKILL.md
      - name: Analyze issue with MiMo Code
        id: mimo
        continue-on-error: true
        uses: AzideCupric/ai-issue-analysis@mimocode
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          mimo-api-key: ${{ secrets.MIMO_API_KEY }}
          mimo-base-url: ${{ secrets.MIMO_BASE_URL }}
          mimo-model: ${{ secrets.MIMO_MODEL }}
          bot-name: "@github-actions"

      - name: 查看完整输入输出
        if: always()
        env:
          ISSUE_NUMBER: ${{ steps.mimo.outputs.issue-number }}
          COMMENT_ID: ${{ steps.mimo.outputs.comment-id }}
          COMMENT_URL: ${{ steps.mimo.outputs.comment-url }}
        run: |
          echo "issue_number=$ISSUE_NUMBER"
          echo "comment_id=$COMMENT_ID"
          echo "comment_url=$COMMENT_URL"
          echo "(Full mimo-output and final-conclusion are available in the uploaded artifacts)"
